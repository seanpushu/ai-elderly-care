'use strict';
const form=document.querySelector('#contact-form'),nameInput=document.querySelector('#name'),phoneInput=document.querySelector('#phone'),statusText=document.querySelector('#contact-status'),callLink=document.querySelector('#call');
const key='pausepal.trusted-contact.v1';
function showContact(name,phone){callLink.hidden=true;callLink.removeAttribute('href');if(!name||!/^\+?[\d ()-]{7,30}$/.test(phone)||phone.replace(/\D/g,'').length<7)return false;nameInput.value=name;phoneInput.value=phone;statusText.textContent=`Saved contact: ${name} — ${phone}`;callLink.href='tel:'+phone.replace(/[ ()-]/g,'');callLink.textContent=`Open dialer for ${name}`;callLink.hidden=false;return true;}
try{const saved=JSON.parse(sessionStorage.getItem(key)||'null');if(saved)showContact(String(saved.name||''),String(saved.phone||''));}catch{statusText.textContent='Session storage is unavailable. You can still use the verification steps.';}
form.addEventListener('submit',event=>{event.preventDefault();const name=nameInput.value.trim(),phone=phoneInput.value.trim();if(!showContact(name,phone)){statusText.textContent='Enter a name and a valid phone number you already trust.';return;}try{sessionStorage.setItem(key,JSON.stringify({name,phone}));}catch{statusText.textContent='This contact is shown here, but could not be saved for this session.';}});
document.querySelector('#clear').addEventListener('click',()=>{form.reset();callLink.hidden=true;callLink.removeAttribute('href');try{sessionStorage.removeItem(key);statusText.textContent='Contact cleared.';}catch{statusText.textContent='The form was cleared, but stored data could not be removed. Close this tab to end the session.';}});
document.querySelector('#copy').addEventListener('click',async()=>{const feedback=document.querySelector('#copy-status');try{await navigator.clipboard.writeText(document.querySelector('#script').textContent);feedback.textContent='Copied. You can send it yourself using a trusted channel.';}catch{feedback.textContent='Copy is unavailable. Please select and copy the message above.';}});
